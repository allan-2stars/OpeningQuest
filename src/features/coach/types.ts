export type CoachCharacter = "SIR_KNIGHT";

export type CoachMessage = {
  character: CoachCharacter;
  title: string;
  message: string;
};

export type CoachCharacterMeta = {
  name: string;
  avatar: string;
};
